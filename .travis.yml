language: python
python:
  - "2.7"

branches:
  only:
  - master
  - develop

install:
  - sudo sudo apt-get install -y pylint

# command to run tests
script:
  - echo "pylint src/logni.py ... "
  - pylint logni.py

  - echo "run logni.py ... "
  - python logni.py

